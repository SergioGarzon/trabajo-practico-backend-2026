version: '3.8'

services:
  mysql-db:
    image: mysql:8.0
    container_name: contenedor-mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: usuarios_db
      MYSQL_USER: root
